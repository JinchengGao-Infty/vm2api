# Infty 的 vm2api 维护分支

- 用户决定自行维护修复。工作分支为 `infty/main`，推送到 `fork`（JinchengGao-Infty/vm2api）；`origin`（dofastted/vm2api）用于获取上游更新。除非用户再次明确要求，不向上游提交 PR、issue 或评论。
- 开工先读 `MAINTENANCE.md`，检查工作区和分支；保留已有改动。更新上游时合并到维护分支，不用上游分支或 release tag 覆盖维护分支。
- 上游实现可用时以上游为准，不保留已被上游修复取代的自用代码补丁。v1.3.9 已修复安装、同步和模板制作的主内核优先级；模板晋升采用上游复制所选槽快照的语义。部署配置与代码分别维护，保留用户的账号、出口、额度和缓存配置。
- v1.3.21 已取代本地配置权限和缓存 TTL 两处修复，应用代码采用上游原版。没有必要补丁时优先拉取固定版本的 `ghcr.io/dofastted/vm2api:v<版本>`，不重建相同源码镜像；维护分支继续保存部署记录。
- 代码更新、模板/槽位同步与运行中内核重启是不同动作。部署先查实际运行状态，保留回退文件，不打断正在进行的用户任务。不能把源码更新当作运行中修复已生效。
- 调试围绕实际受影响路径，不新增无关测试、不跑全套回归、不计算哈希。缓存效果用连续请求的 cache read/write 与 TTL 观察；不要仅凭面板配置宣称生效。
- 凭证和原始会话请求保存在仓库之外，不提交到 Git。
